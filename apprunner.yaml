version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt
run:
  command: python -m src.beer_clanker_bot.api
  network:
    port: 8000
  env:
    - name: PORT
      value: "8000"
